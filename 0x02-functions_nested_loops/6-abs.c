#include "main.h"

/**
 * _abs - main entry
 *
 * Descriptio: computes the absolute value of an int
 *
 * @n: the int to check
 *
 * Return: absolute value
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		ruturn(n);
	}
	return (-n);
}

#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: contains the number of commands when we run
 * the program
 * @argv: string that contains the comands when we
 * run the program
 *
 * Return: 0.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

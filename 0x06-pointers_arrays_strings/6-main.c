#include "main.h"
#include <stdio.h>

/**
 * main - check code
 *
 * Return: Always 0.
 */
int main(void)
{
	char str[] = "Expect the best. Prepare for the worst. Capitalize on what comes. \nHello world! hello-world 012345hello world\thello world.hello world\n";
	char *ptr;

	ptr = cap_string(str);
	printf("%s", prtr);
	printf("%s", str);
	return (0);
}

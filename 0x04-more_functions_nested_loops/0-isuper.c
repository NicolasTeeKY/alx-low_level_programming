#include "main.h"

/**
 * _isuper - checks for uppercase character
 * @c: the chariacter to be checked
 * Return: 1 if c is uppercase , 0 otherwise
 */

int _isuper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
